# Benchmark

The first real benchmark is the supplied AWS DevOps Blog article about Kiro, Amazon Bedrock Knowledge Bases, LangChain, and MCP.

The source explicitly describes Kiro -> MCP server -> Retrieve API and the retrieval path through Titan Text Embeddings v2 and OpenSearch Serverless. rkeytect must preserve those facts as evidence-backed architecture rather than treating all model output as truth.
