## Real AWS benchmark

Use `examples/aws-blogs/kiro-bedrock-mcp.json` with the article-ingestion pipeline. The benchmark covers Kiro, MCP, Amazon Bedrock Knowledge Bases, the Retrieve API, Amazon Titan Text Embeddings v2, and Amazon OpenSearch Serverless.

The article explicitly describes the retrieval sequence, so this is a good early test of evidence-backed relationship extraction before adding an LLM.
