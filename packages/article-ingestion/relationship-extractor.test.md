# Relationship extractor acceptance criteria

For the Kiro benchmark, an acceptable Level-1 result contains explicit evidence-backed relationships for `connects`, `calls`, `embeds`, and `searches`. A service mention without a matching relationship sentence must remain a component only.
